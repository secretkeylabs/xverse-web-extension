export enum ImportKeystoneSteps {
  START = 0,
  CONNECT_KEYSTONE = 1,
  ADD_MULTIPLE_ACCOUNTS = 2,
  ADDRESS_ADDED = 3,
  ADD_ACCOUNT_NAME = 4,
  IMPORT_END = 5,
}

export enum LedgerLiveOptions {
  USING = 'using',
  NOT_USING = 'not using',
}
